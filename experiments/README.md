# Experiments

This folder holds explicit, rerunnable research and migration programs that do
not belong in the importable library. `build_instances.py` documents how the
initial thesis and archived-log instances were converted to canonical JSON.

Core correctness claims belong in `tests/`, and normal runs belong in the
`fka` CLI. Avoid adding notebooks or unstructured output logs.
